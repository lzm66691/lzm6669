# Issue tracker: markdown

本仓库的任务后端是**本地 Markdown**（不使用 GitHub/GitLab）。

## 票据位置

| 内容 | 路径 |
| --- | --- |
| 规格 | `.scratch/spec.md` |
| 地图 | `.scratch/map.md` |
| 票据 | `.scratch/issues/<序号>-<slug>.md` |

## 标签词表

`bug` · `needs-triage` · `needs-info` · `ready-for-agent` · `ready-for-human` · `wontfix`
